{
  "name": "the-llm-dispatcher",
  "version": "0.1.0",
  "description": "An LLM router that uses Jev (TypeSafe System One) to dispatch each request to the cheapest model and lowest reasoning effort that will do the job. OpenAI-compatible proxy and library.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": { "llm-dispatcher": "./dist/cli.js" },
  "files": ["dist", "../models.json"],
  "engines": { "node": ">=20.12" },
  "scripts": {
    "serve": "tsx src/cli.ts serve",
    "route": "tsx src/cli.ts route",
    "models": "tsx src/cli.ts models",
    "stats": "tsx src/cli.ts stats",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
